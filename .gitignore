__pycache__/
*.pyc
*.pyo
output/
out/
.pytest_cache/
pytest-cache-files-*/
*.egg-info/
.DS_Store
data/sample/*_BK*/
data/worldmap_cache.db 

# note article deliverables & screenshots (local-only, not versioned)
/*.docx
/*.jpg
/*.jpeg
/*.png

# keep these explicitly, even though they match the patterns above
!/WOM_*.docx
!/Oil_Global_SC_design_draft.md

# commit message drafts (local-only; passed to `git commit -F`)
/commit_msg*.txt

# Phase 7 の層間ハンドオフが生成する需要CSV（allocation_id ごと・再生成可能）
data/sample/*/demand_forecast_A*.csv
